FROM insurgency-base:latest

ENV APPID 237410
ENV APPDIR /home/steamsrv/237410
ENV APP_GAME_NAME insurgency
ENV APP_SRCDS_FLAGS ""
ENV APP_SERVER_PORT 27018
ENV APP_SERVER_MAXPLAYERS 24
ENV APP_SERVER_MAP market_coop
ENV APP_SERVER_NAME insurgencyServer
ENV USE_SRCDS true

EXPOSE ${APP_SERVER_PORT}
EXPOSE ${APP_SERVER_PORT}/udp

CMD ["/bin/run_server"]
